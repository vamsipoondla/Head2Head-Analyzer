/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Enable static file serving from public/
  // CSV data will be served from /data/1926-2024_COMBINED_NFL_SCORES.csv
};

module.exports = nextConfig;
